import { Theme } from '../interfaces/theme'

const light: Theme = {
    color: {
        background: "#fff",
        buttonBackground: '#fff',
        buttonColor: '#2161f2'
    }
}

export default light